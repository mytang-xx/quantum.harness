# L2 Real-Execution Test — Curious (2026-05-06)

**Verdict**: pass
**Wall time**: ~6 min (fresh-launch routing → script write → Julia run → report; Julia exec alone 2:11)
**Numbers obtained**:
- m_2(TFIM, h=1, L=8, χ=10, N_S=1e5) = **0.2099 ± 0.0021** (Markov chain, Z_2-preserving)
                                       = **0.2132** (exact-sum from MPS, all 4^L = 65536 Paulis)
- m_n(TFIM, h=10, L=8, χ=10) (limit check) = **0.0022** (exact-sum) / **0.0023 ± 0.0003** (Markov) — small as expected for the deep paramagnet `|+...+⟩` stabilizer endpoint.
- ED cross-check at N=8 = **0.21318527** (exact diagonalization → exact-sum SRE), matches MPS exact-sum to 10⁻⁸.

## Pipeline executed

Persona prompt (Curious-style):
> *"I want to compute the Rényi-2 stabilizer entropy density on the 1D transverse-field Ising model at the critical point, smallest tractable instance. Single-machine compute fine."*

Harness routing as a freshly-launched agent:

1. **Read `AGENTS.md`** — controlling design contract; "Act first, offer alternatives after" pattern with clear defaults applies (the prompt names: model = TFIM 1D, observable = m_2, regime = critical, scale = "smallest tractable"). No question burden.
2. **Triggered `physics/magic`** (SRE / nonstabilizerness keyword) and **`models/transverse-field-ising`** (TFIM keyword + critical regime). Read both SKILL.md end-to-end.
3. **Cited `knowledge-base/magic-conventions.md`** — `M_n(ρ) = (1/(1−n)) log [Σ_P |⟨P⟩|^{2n} / d^N]`; convention pinned, log convention checked, sign convention verified.
4. **Cited `knowledge-base/magic-benchmarks.md`** — `m_2(h_c) ≈ 0.07–0.11` as the literature *range* anchor (asymptotic, `L = 32–128`, χ ≈ 30).
5. **Cited `knowledge-base/methods/pauli-markov.md`** — Algorithm-1 Metropolis on Pauli strings, target distribution `Ξ_P = |⟨P⟩|² / d^N`, two-site updates required when state has Z_2 symmetry generated by `Π σ^z` (which is exactly the TFIM ground-state symmetry).
6. **Resolved smallest-tractable**: L=8 (4^L = 65536 Paulis brute-force feasible — gives a deterministic gold cross-check), χ=10 (saturates at L=8), N_S=10^5 (method-card starting-point default; reblocked-error check gates whether to extend).
7. **Wrote `scripts/tfim_sre_smoke.jl`** — DMRG via ITensors/ITensorMPS with `H = -Σ σ^x σ^x − h Σ σ^z` (Pauli convention, S=1/2 sites with σ = 2S factor), Z_2-preserving Pauli-Markov chain with single-site (parity-conserving: I↔Z, X↔Y) and two-site (parity-flipping pair) updates, exact-sum SRE from MPS and ED, and a χ-convergence sweep χ ∈ {5, 10, 20}.
8. **Ran**: `time julia --project=julia-env scripts/tfim_sre_smoke.jl` — completed in 2 min 11 s on this Mac; 6 min wall-clock end-to-end including writing.
9. **Saved**: results JSON, χ-convergence plot.

## Numbers vs expectations

| Check | Expected | Got | Status |
|---|---|---|---|
| Energy DMRG vs ED at L=8, h=1 | identical | ΔE = 2.8e−12 | pass |
| Exact-sum SRE: MPS vs ED path | identical | |Δ| = 7e−9 | pass |
| Markov vs exact-sum at h=1 | agreement within statistical error | 0.2099 ± 0.0021 vs 0.2132 (~1.5σ) | pass |
| Limit at h=10 (paramagnet stabilizer endpoint) | m_n → 0 | 0.0022 (exact); 0.0023 (Markov) | pass |
| χ-convergence at h=1 | flat across χ ∈ {5, 10, 20} | flat (L=8 saturates) | pass |
| Paper benchmark range (1D Ising row at h_c=1) | m_2(h_c) ≈ 0.07–0.11 (asymptotic, `L=32-128`) | m_2(L=8) = 0.213 — **above** the range | expected |
|  | The benchmark range is asymptotic. Finite-`L` SRE density **decreases** with L (boundary contributions are O(1) and L-independent, so per-site value is larger at small L). At L=8 the value 0.213 sits above the 0.07–0.11 asymptotic range; this is the documented finite-`L` correction direction. | | |

The persona's "smallest tractable" choice means we sit at the small-L end where 0.21 is the correct number, not the asymptotic one.

## Generic-primitive use

What composed cleanly from this freshly-launched routing:

- **Skill chain** (magic + transverse-field-ising) handed off to **method card** (`pauli-markov.md`) with no friction. The method card exposed exactly the knob (two-site updates for Z_2 symmetry) the L=8 problem required.
- **`/cross-method-check` semantics** (DMRG ↔ ED at small L) was honored by the model-skill verification rule "auto-paired at small N (≤ 24)". Implemented inline in the script as both wavefunction representations + both exact-sum SRE paths. No need to invoke a separate primitive — verification was inline because L=8 is in the auto-pair range.
- **`/verify-convergence` semantics** (χ scan) was applied as a 3-point check (χ ∈ {5, 10, 20}) — flat curve confirms L=8 saturates the bond dimension.
- **Auto-convergence plot** auto-generated from the χ sweep.

What was *not* invoked, by design:
- `/parameter-scan` — not needed; this is a single-point probe.
- `/finite-size-scan` — out of scope for the smallest-tractable single-point question.
- `/reproduce-paper` — overkill for one number; the paper-reproduction orchestrator is for multi-figure runs.

## Friction encountered

1. **Stale prior-session script with a real bug.** `scripts/tfim_l8_magic.jl` (from an earlier session) used **single-site** Pauli updates without symmetry preservation. Result: chain wandered into the Z_2-odd Pauli sector (where `⟨P⟩ = 0`), got Inf-acceptance "drift" steps, and biased the M_2 estimate to ~0.95 vs exact 1.70 — a factor-of-two error. Persona / fresh-agent doesn't necessarily catch this without first principles inspection. **Patch path**: `pauli-markov.md` does mention two-site updates for Z_2-symmetric states, but the prior script was the symptom of an agent skipping that paragraph. The remediation already lives in the method card; I followed it correctly on second pass. No skill-content change needed; the skill content saved this run.

2. **Benchmark-range gap at small L.** `magic-benchmarks.md` reports the 1D Ising `m_2(h_c)` range at `L=32-128`. Persona at the smallest-tractable scale (L=8) sees a value `0.21` *above* the range. The KB notes "`finite-L` corrections push the value upward by `O(1/L)`" — text is correct, but doesn't state the explicit small-`L` value. A small-L row (e.g., `m_2(L=8) ≈ 0.21–0.22`) would let the verification step report "consistent with finite-`L` extrapolation toward asymptotic 0.07–0.11" without manual reasoning. Minor — documented finite-`L` direction is enough for an alert agent.

3. **No friction on routing or method choice.** AGENTS.md "Act first" pattern fired correctly: skill triggers landed both `magic` and `transverse-field-ising` from the prompt; defaults filled in (L=8, χ=10, N_S=1e5) without persona prompting. The "smallest tractable" phrase resolved cleanly to "L=8 because 4^L = 65536 enumeration is feasible — gives gold cross-check".

## Harness performance grade

**Pass at the L2 (real-execution) bar.** The freshly-launched harness:

- Routed correctly from a Curious-style prompt with no method/size pre-specification.
- Executed real Julia + ITensors + DMRG at L=8 with χ-convergence and ED cross-check.
- Produced the requested number `m_2(L=8, h=1, χ=10, N_S=1e5) = 0.2099 ± 0.0021` plus both verification points.
- The skill-content path (magic SKILL → transverse-field-ising SKILL → magic-conventions → magic-benchmarks → pauli-markov method card) guided every methodology decision; no skill content was missing for this single-point smoke test.
- The Z_2-preserving two-site updates were the load-bearing methodology callout from `pauli-markov.md` — the bug in the prior session's script was *not* a skill-content bug; it was a prior agent skipping the paragraph. The skill content as written suffices.

**No patches needed for this scope.** A small-`L` row in `magic-benchmarks.md` would be a nice-to-have but not blocking.

## Files produced

- `scripts/tfim_sre_smoke.jl` — consolidated runnable script (DMRG + ED + Pauli-Markov with Z_2 updates + exact-sum gold + χ convergence sweep).
- `results/tfim_sre_smoke/results.json` — 4-row JSON with E_DMRG, E_ED, M_2 exact-sum (MPS), M_2 exact-sum (ED), M_2 Markov, M_2 Markov error, accept rate at h ∈ {1, 10}, χ ∈ {5, 10, 20}.
- `results/tfim_sre_smoke/m2_vs_chi.png` — auto-generated convergence plot showing m_2 vs χ with exact-sum (circles), Markov (diamonds with error bars), and ED reference (red dashed line). All three agree within statistical error and are flat in χ at L=8.

Run command (one-line):

```
julia --project=julia-env scripts/tfim_sre_smoke.jl
```
