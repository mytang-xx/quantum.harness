raw/
.raw/
.zulip/
.DS_Store
# Only Project.toml is committed (the dependency contract). Manifest.toml is
# platform/version-pinned (see `julia_version` field) and would break
# cross-machine syncing — each environment resolves its own Manifest from
# Project.toml on first instantiate.
julia-env/Manifest.toml
julia-env/.CondaPkg/
.julia/
.knowledge/literature/**/.raw/
.knowledge/literature/**/.figures/
tracks/**/.raw/
tracks/**/.figures/
tracks/**/results/

# Per-method JSON manifests are derived from .knowledge/literature/ref.bib
# via skills/download-ref/helpers/bibtex_to_manifest.py — don't sync.
.knowledge/literature/manifests/

# Managed by Ion
.claude/skills/
CLAUDE.md

# Remote skills managed by Ion. Ion recreates these symlinks from Ion.toml/Ion.lock.
skills/arxiv-search
skills/brainstorming
skills/context7-cli
skills/dispatching-parallel-agents
skills/executing-plans
skills/finishing-a-development-branch
skills/idea-writer
skills/ideas
skills/ion-cli
skills/receiving-code-review
skills/requesting-code-review
skills/scientific-visualization
skills/subagent-driven-development
skills/survey
skills/systematic-debugging
skills/test-driven-development
skills/using-git-worktrees
skills/using-superpowers
skills/verification-before-completion
skills/writing-plans
skills/writing-skills

# Claude runtime state
.claude/scheduled_tasks.lock
.claude/cache/
scripts/_*
/results/
/tmp/
/.external/
.venv-tenpy/
.pytest_cache/
.coverage
__pycache__/
target/
.superpowers/
docs/superpowers/

# Per-user cluster profiles (connection secrets: host/IP, user, key paths, local
# paths). Cluster cards (*.md) carry the same identifying info. The committed
# localhost.toml stays as the public example; each user's real profile + active
# pointer are local-only.
skills/using-slurm/profiles/hkust-gz.toml
skills/using-slurm/profiles/hkust-gz.md
skills/using-slurm/profiles/active.toml

# Git worktrees
.worktrees/

# Cryochamber runtime (per-machine state). The chamber *design* — .cryo/plan.md,
# .cryo/cryo.toml, .cryo/README.md — is committed; everything below is local.
# Advisor drafts under .zulip/.drafts/ are already covered by the .zulip/ rule.
.cryo/NOTES.md
.cryo/messages/
.cryo/*.log
.cryo/timer.json
.cryo/todo.json
.cryo/.cryo/
.cryo/tmp/
